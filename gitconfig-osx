; NOTE:
; To load this file, add the following to "~/.gitconfig-local":
; [include]
;     path = ~/.gitconfig-osx

[core]
    ; NOTE: For the option -F to work properly we need a newer version of less ("brew install less" on OSX)
    pager = /usr/local/share/git-core/contrib/diff-highlight/diff-highlight | less -SF

; vim: ft=gitconfig
